anthropic>=0.40.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
psycopg2-binary>=2.9.9  # optional: only used if DATABASE_URL is set, see backend/db.py

# dev/test only
pytest>=8.0.0
httpx>=0.27.0
